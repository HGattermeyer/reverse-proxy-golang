package data

import "proxy-golang/internal/models"

// server slice to register the servers to redirect
var ServerSlice []models.Server

// PathPrefix is the common path prefix for API endpoints
var PathPrefix = "/api/v1"
